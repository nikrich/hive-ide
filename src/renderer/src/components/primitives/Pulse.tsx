/**
 * Animated dot used by `StatusChip` for the running state.
 *
 * Styling lives entirely in `styles/ide.css` under `.pulse` — this component
 * is just the marker element.
 */
export function Pulse() {
  return <span className="pulse" />
}
